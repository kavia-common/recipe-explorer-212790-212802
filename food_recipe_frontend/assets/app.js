(function(){
  // Shared minimal JS. Could be used for navigation hooks or generic helpers.
  window.FigmaScreen = {
    init: function(){ /* no-op for now */ }
  };
  document.addEventListener('DOMContentLoaded', function(){
    window.FigmaScreen.init();
  });
})();
